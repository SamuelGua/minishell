bash-5.1$ << "$HOME" cat
> de
> de
> $HOME

retour 
de
de

le HOME n'est pas a expand

---------------------------

bash-5.1$ << $HOME"A" cat
de
de
$PWD
$HOMEA

retour
de
de
$PWD

bash-5.1$ << "$HOME" cat
de
de
$PWD

retour
de
de
$PWD

Pas d'expand de PWD

----------------------
bash-5.1$ << $HOME cat
de
de
$PWD
$HOME

retour 
de
de
/home/scely/Documents/C-CURSUS/minishell

*** il n'y pas de delimiteur n'est pas quoted, le PWD est a expand